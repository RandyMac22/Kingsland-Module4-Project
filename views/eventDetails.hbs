{{> header}}
<div class="row event-details">
    <div class="col-md-12 text-center overflow-hidden">
      <img id="img" class="details-img"
        src ={{imgURL}} />
      <div class="my-3 p-3">
        <h2 class="display-5">{{name}}</h2>
        <p class="infoType">Description:</p>
        <p class="event-description">{{description}}</p>
        <p class="infoType">Date: <small>{{dateTime}}</small></p>
        <p class="infoType">People interested in: <small>{{peopleInterestedIn}}</small></p>
        <p class="infoType">Organizer: <small>{{creator}}</small></p>
      </div>
      
      <a href="#/join/{{id}}" class="btn btn-info btn-lg">Join the event</a>
      
    </div>
</div>
{{> footer}}